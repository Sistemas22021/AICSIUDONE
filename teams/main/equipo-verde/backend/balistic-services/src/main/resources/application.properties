# ============================================================
# EJEMPLO DE CONFIGURACIÓN - Copia este archivo como:
#   application.properties
# y reemplaza los valores con tus credenciales locales.
# ============================================================

# --- Almacenamiento ---
storage.location=${STORAGE_LOCATION_BALISTIC:uploads}
storage.type=${STORAGE_TYPE:local}

# --- Base de Datos (Supabase PostgreSQL) ---
spring.datasource.url=jdbc:postgresql://aws-0-ca-central-1.pooler.supabase.com:6543/postgres?prepareThreshold=0
spring.datasource.username=postgres.nknbplmujgwjsjusqrux
spring.datasource.password=VerdeDev.2026
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.properties.hibernate.hbm2ddl.jdbc_metadata_extraction_strategy=individually
spring.sql.init.mode=${SQL_INIT_MODE:never}
spring.jpa.defer-datasource-initialization=true
spring.jpa.show-sql=${JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.org.hibernate.envers.store_data_at_delete=false
spring.jpa.properties.org.hibernate.envers.audit_table_suffix=_aud
spring.jpa.properties.org.hibernate.envers.revision_field_name=rev
spring.jpa.properties.org.hibernate.envers.revision_type_field_name=revtype

# --- Puerto de la API local ---
server.port=${SERVER_PORT_BALISTIC:8081}

# AWS S3 / LocalStack Configuration
aws.s3.endpoint=${AWS_S3_ENDPOINT:http://127.0.0.1:4566}
aws.s3.region=${AWS_S3_REGION:us-east-1}
aws.s3.access-key=${S3_ACCESS_KEY_BALISTIC:test}
aws.s3.secret-key=${S3_SECRET_KEY_BALISTIC:test}
aws.s3.bucket-name=${AWS_S3_BUCKET:sistema-balistico-images}